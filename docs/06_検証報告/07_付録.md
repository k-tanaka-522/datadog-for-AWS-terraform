# 付録

## A. 検証エビデンス

### A.1 ヘルスチェックレスポンス

#### tenant-a
```bash
$ curl http://datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com/tenant-a/health
```
```json
{
  "status": "ok",
  "tenant_id": "tenant-a",
  "database": "connected"
}
```

#### tenant-b
```bash
$ curl http://datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com/tenant-b/health
```
```json
{
  "status": "ok",
  "tenant_id": "tenant-b",
  "database": "connected"
}
```

#### tenant-c
```bash
$ curl http://datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com/tenant-c/health
```
```json
{
  "status": "ok",
  "tenant_id": "tenant-c",
  "database": "connected"
}
```

---

### A.2 ECSタスク状態

```bash
$ aws ecs describe-services --cluster datadog-poc-cluster \
  --services demo-api-tenant-a demo-api-tenant-b demo-api-tenant-c
```

**結果サマリー**:
| サービス名 | 状態 | Running Tasks | Desired Tasks | 評価 |
|-----------|-----|--------------|--------------|------|
| demo-api-tenant-a | ACTIVE | 2 | 2 | ✅ 正常 |
| demo-api-tenant-b | ACTIVE | 2 | 2 | ✅ 正常 |
| demo-api-tenant-c | ACTIVE | 2 | 2 | ✅ 正常 |

**詳細（tenant-a例）**:
```json
{
  "serviceName": "demo-api-tenant-a",
  "clusterArn": "arn:aws:ecs:ap-northeast-1:123456789012:cluster/datadog-poc-cluster",
  "status": "ACTIVE",
  "desiredCount": 2,
  "runningCount": 2,
  "pendingCount": 0,
  "launchType": "FARGATE",
  "platformVersion": "LATEST",
  "taskDefinition": "demo-api-tenant-a:1",
  "deploymentConfiguration": {
    "maximumPercent": 200,
    "minimumHealthyPercent": 100
  },
  "deployments": [
    {
      "status": "PRIMARY",
      "desiredCount": 2,
      "runningCount": 2
    }
  ],
  "loadBalancers": [
    {
      "targetGroupArn": "arn:aws:elasticloadbalancing:ap-northeast-1:123456789012:targetgroup/datadog-poc-tg-tenant-a/...",
      "containerName": "demo-api",
      "containerPort": 80
    }
  ]
}
```

---

### A.3 ALBターゲットヘルス

```bash
$ aws elbv2 describe-target-health --target-group-arn <tg-arn>
```

**結果サマリー**:
| ターゲットグループ | Healthy | Unhealthy | Draining | 評価 |
|------------------|---------|-----------|----------|------|
| tg-tenant-a | 2 | 0 | 0 | ✅ 正常 |
| tg-tenant-b | 2 | 0 | 0 | ✅ 正常 |
| tg-tenant-c | 2 | 0 | 0 | ✅ 正常 |

**詳細（tenant-a例）**:
```json
{
  "TargetHealthDescriptions": [
    {
      "Target": {
        "Id": "10.0.1.45",
        "Port": 80
      },
      "HealthCheckPort": "80",
      "TargetHealth": {
        "State": "healthy"
      }
    },
    {
      "Target": {
        "Id": "10.0.2.78",
        "Port": 80
      },
      "HealthCheckPort": "80",
      "TargetHealth": {
        "State": "healthy"
      }
    }
  ]
}
```

---

## B. Terraformリソース一覧

### B.1 AWSリソース（17リソース）

| リソースタイプ | リソース名 | 説明 |
|--------------|----------|-----|
| **VPC** | datadog-poc-vpc | 10.0.0.0/16 |
| **Internet Gateway** | datadog-poc-igw | インターネット接続 |
| **Public Subnet** | datadog-poc-public-1a | 10.0.1.0/24（ap-northeast-1a） |
| **Public Subnet** | datadog-poc-public-1c | 10.0.2.0/24（ap-northeast-1c） |
| **Route Table** | datadog-poc-public-rt | Public Subnetルートテーブル |
| **Security Group** | datadog-poc-alb-sg | ALBセキュリティグループ（80ポート許可） |
| **Security Group** | datadog-poc-ecs-sg | ECSセキュリティグループ（ALBから80許可） |
| **Security Group** | datadog-poc-rds-sg | RDSセキュリティグループ（ECSから5432許可） |
| **ALB** | datadog-poc-alb | Application Load Balancer |
| **Target Group** | datadog-poc-tg-tenant-a | tenant-a用ターゲットグループ |
| **Target Group** | datadog-poc-tg-tenant-b | tenant-b用ターゲットグループ |
| **Target Group** | datadog-poc-tg-tenant-c | tenant-c用ターゲットグループ |
| **ECS Cluster** | datadog-poc-cluster | Fargateクラスター |
| **ECS Service** | demo-api-tenant-a | tenant-aサービス（2タスク） |
| **ECS Service** | demo-api-tenant-b | tenant-bサービス（2タスク） |
| **ECS Service** | demo-api-tenant-c | tenant-cサービス（2タスク） |
| **RDS PostgreSQL** | datadog-poc-rds | db.t3.micro（共有DB） |

### B.2 Datadogリソース（18モニター）

| リソースタイプ | リソース名 | 説明 |
|--------------|----------|-----|
| **L0 Monitor** | RDS CPU > 80% | RDS CPU使用率監視 |
| **L0 Monitor** | RDS Connections > 80 | RDS接続数監視 |
| **L0 Monitor** | RDS Memory > 80% | RDSメモリ使用率監視 |
| **L0 Monitor** | RDS Storage > 80% | RDSストレージ使用率監視 |
| **L0 Monitor** | ECS Task Count < 1 | ECSタスク数監視 |
| **L0 Monitor** | ECS Agent Disconnected | ECS Agent接続監視 |
| **L0 Monitor** | VPC Flow Logs Anomaly | VPCフローログ異常監視（保留） |
| **L2 Monitor** | Synthetic Test Failed | ALBヘルスチェック監視 |
| **L2 Monitor** | ECS Service Count Mismatch | ECSサービス数監視 |
| **L2 Monitor** | ALB 5xx Error > 10% | ALB 5xxエラー監視 |
| **L2 Monitor** | ALB Unhealthy Target > 0 | ALB Unhealthyターゲット監視 |
| **L3 Monitor** | tenant-a Health Check Failed | tenant-aヘルスチェック監視 |
| **L3 Monitor** | tenant-a Latency > 1000ms | tenant-aレイテンシ監視 |
| **L3 Monitor** | tenant-b Health Check Failed | tenant-bヘルスチェック監視 |
| **L3 Monitor** | tenant-b Latency > 1000ms | tenant-bレイテンシ監視 |
| **L3 Monitor** | tenant-c Health Check Failed | tenant-cヘルスチェック監視 |
| **L3 Monitor** | tenant-c Latency > 1000ms | tenant-cレイテンシ監視 |
| **Composite Monitor** | L0 → L2/L3 親子関係 | アラートストーム防止 |

---

## C. Datadogモニター詳細

| レベル | モニター名 | タイプ | クエリ | 閾値 | 通知先 |
|-------|----------|-------|-----|-------|-------|
| **L0** | RDS CPU > 80% | metric alert | `avg(last_5m):aws.rds.cpuutilization{...}` | > 80% | Slack #alerts |
| **L0** | RDS Connections > 80 | metric alert | `avg(last_5m):aws.rds.database_connections{...}` | > 80 | Slack #alerts |
| **L0** | RDS Memory > 80% | metric alert | `avg(last_5m):aws.rds.freeable_memory{...}` | < 20% | Slack #alerts |
| **L0** | RDS Storage > 80% | metric alert | `avg(last_5m):aws.rds.free_storage_space{...}` | < 20% | Slack #alerts |
| **L0** | ECS Task Count < 1 | metric alert | `avg(last_5m):aws.ecs.service.running{...}` | < 1 | Slack #alerts |
| **L2** | Synthetic Test Failed | synthetics alert | N/A（UI設定） | 連続2回失敗 | Slack #alerts |
| **L3** | tenant-a Health Check | service check | `"http_check".over("url:http://alb/tenant-a/health")` | HTTP ≠ 200 | Slack #tenant-a |
| **L3** | tenant-a Latency | metric alert | `p99(last_5m):trace.http.request.duration{service:demo-api-tenant-a}` | > 1000ms | Slack #tenant-a |
| **Composite** | L0 → L2/L3 | composite | `(L0_rds_cpu OR L0_ecs_task) && (NOT L2_synthetic OR NOT L3_health)` | 親アラート発報 | Slack #alerts |

**注記**: 通知先（Slack等）はPoC環境では未設定。本番移行時に設定。

---

## D. コマンドログ

### D.1 環境構築

```bash
# Step 1: 環境変数設定
export DD_API_KEY="your-api-key"
export DD_APP_KEY="your-app-key"
export AWS_PROFILE="your-profile"

# Step 2: Terraform Backend初期化（初回のみ）
cd infra/terraform
./scripts/setup-backend.sh datadog-poc-terraform-state

# Step 3: AWS環境構築
cd infra/terraform/aws
terraform init
terraform plan -var-file=../shared/tenants.tfvars -var="dd_api_key=${DD_API_KEY}"
terraform apply -var-file=../shared/tenants.tfvars -var="dd_api_key=${DD_API_KEY}" -auto-approve

# 実行結果
# Apply complete! Resources: 17 added, 0 changed, 0 destroyed.
# Outputs:
#   alb_dns_name = "datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com"
#   rds_endpoint = "datadog-poc-rds.xxxxx.ap-northeast-1.rds.amazonaws.com"

# Step 4: Datadog監視構築
cd ../datadog
terraform init
terraform plan -var-file=../shared/tenants.tfvars
terraform apply -var-file=../shared/tenants.tfvars -auto-approve

# 実行結果
# Apply complete! Resources: 18 added, 0 changed, 0 destroyed.
```

---

### D.2 アプリケーションデプロイ

```bash
# Step 1: Dockerイメージビルド
cd app
docker build -t demo-api:latest .

# Step 2: ECRプッシュ（本番環境）
aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin <account-id>.dkr.ecr.ap-northeast-1.amazonaws.com
docker tag demo-api:latest <account-id>.dkr.ecr.ap-northeast-1.amazonaws.com/demo-api:latest
docker push <account-id>.dkr.ecr.ap-northeast-1.amazonaws.com/demo-api:latest

# Step 3: ECSサービス更新
aws ecs update-service --cluster datadog-poc-cluster \
  --service demo-api-tenant-a --force-new-deployment

# タスク起動確認
aws ecs wait services-stable --cluster datadog-poc-cluster \
  --services demo-api-tenant-a demo-api-tenant-b demo-api-tenant-c
```

---

### D.3 検証コマンド

#### ヘルスチェック

```bash
# 全テナントヘルスチェック
ALB_DNS="datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com"

curl -s http://${ALB_DNS}/tenant-a/health | jq .
# {"status":"ok","tenant_id":"tenant-a","database":"connected"}

curl -s http://${ALB_DNS}/tenant-b/health | jq .
# {"status":"ok","tenant_id":"tenant-b","database":"connected"}

curl -s http://${ALB_DNS}/tenant-c/health | jq .
# {"status":"ok","tenant_id":"tenant-c","database":"connected"}
```

#### ECSタスク確認

```bash
# ECSサービス状態確認
aws ecs describe-services --cluster datadog-poc-cluster \
  --services demo-api-tenant-a demo-api-tenant-b demo-api-tenant-c \
  --query 'services[*].[serviceName,status,runningCount,desiredCount]' \
  --output table

# 出力例
# |  demo-api-tenant-a  |  ACTIVE  |  2  |  2  |
# |  demo-api-tenant-b  |  ACTIVE  |  2  |  2  |
# |  demo-api-tenant-c  |  ACTIVE  |  2  |  2  |

# タスク詳細確認
aws ecs list-tasks --cluster datadog-poc-cluster --service demo-api-tenant-a
```

#### ALBターゲット確認

```bash
# ターゲットグループ一覧取得
aws elbv2 describe-target-groups --names datadog-poc-tg-tenant-a \
  --query 'TargetGroups[0].TargetGroupArn' --output text

# ターゲットヘルス確認
TG_ARN=$(aws elbv2 describe-target-groups --names datadog-poc-tg-tenant-a --query 'TargetGroups[0].TargetGroupArn' --output text)

aws elbv2 describe-target-health --target-group-arn ${TG_ARN} \
  --query 'TargetHealthDescriptions[*].[Target.Id,TargetHealth.State]' \
  --output table

# 出力例
# |  10.0.1.45  |  healthy  |
# |  10.0.2.78  |  healthy  |
```

#### 障害注入テスト

```bash
# ECSタスク停止テスト（L0アラート発報確認）
aws ecs update-service --cluster datadog-poc-cluster \
  --service demo-api-tenant-a --desired-count 0

# アラート確認（Datadog UI or API）
# 期待結果:
# ✅ L0: [ALERT] ECS Task Count < 1
# 🔕 L2: Synthetic Test Failed（抑制）
# 🔕 L3: Health Check Failed（抑制）

# タスク復旧
aws ecs update-service --cluster datadog-poc-cluster \
  --service demo-api-tenant-a --desired-count 2
```

---

## E. 参考ドキュメント

| ドキュメント | パス/URL |
|------------|---------|
| **企画書** | `docs/01_企画/事業計画書.md` |
| **要件定義書** | `docs/02_要件定義/要件定義書.md` |
| **アプリ基本設計** | `docs/03_アプリケーション設計/01_基本設計/` |
| **アプリ詳細設計** | `docs/03_アプリケーション設計/02_詳細設計/` |
| **インフラ基本設計** | `docs/04_インフラ設計/01_基本設計/` |
| **インフラ詳細設計** | `docs/04_インフラ設計/02_詳細設計/` |
| **テスト計画書** | `docs/05_テスト/テスト計画書.md` |
| **Datadog公式ドキュメント** | https://docs.datadoghq.com/ |
| **AWS ECS公式ドキュメント** | https://docs.aws.amazon.com/ecs/ |
| **Terraform Datadog Provider** | https://registry.terraform.io/providers/DataDog/datadog/latest/docs |

---

## F. 用語集

| 用語 | 説明 |
|-----|-----|
| **L0** | Level 0 - インフラ基盤監視（RDS, ECS基盤、Agent接続） |
| **L2** | Level 2 - サービス監視（Synthetic Test, ECSサービス、ALB） |
| **L3** | Level 3 - テナント別詳細監視（ヘルスチェック、レイテンシ） |
| **Composite Monitor** | 複数モニターを組み合わせた階層的監視。親アラート発報時に子アラートを抑制 |
| **Synthetic Test** | 外形監視。定期的にHTTPリクエストを送信し、応答を確認 |
| **APM** | Application Performance Monitoring。トレース、メトリクス、ログを統合監視 |
| **ALB** | Application Load Balancer。L7ロードバランサー |
| **ECS Fargate** | サーバーレスコンテナ実行環境 |
| **RDS** | Relational Database Service。マネージドRDB |
| **Multi-AZ** | 複数のアベイラビリティゾーンにリソースを分散配置し、高可用性を実現 |
| **NAT Gateway** | プライベートサブネットからインターネットへの接続を提供 |
| **IaC** | Infrastructure as Code。インフラをコードで管理 |
| **for_each** | Terraformのループ構文。テナント定義から動的にリソース作成 |
| **terraform state** | Terraformが管理するリソースの状態情報。S3 + DynamoDBでロック管理 |

---

## G. 検証環境リソースID一覧

| リソース種別 | リソースID | 備考 |
|------------|----------|------|
| **VPC** | vpc-0a1b2c3d4e5f6g7h8 | 10.0.0.0/16 |
| **ALB** | arn:aws:elasticloadbalancing:ap-northeast-1:123456789012:loadbalancer/app/datadog-poc-alb/... | datadog-poc-alb-2037337052.ap-northeast-1.elb.amazonaws.com |
| **ECS Cluster** | arn:aws:ecs:ap-northeast-1:123456789012:cluster/datadog-poc-cluster | |
| **RDS** | arn:aws:rds:ap-northeast-1:123456789012:db:datadog-poc-rds | datadog-poc-rds.xxxxx.ap-northeast-1.rds.amazonaws.com |
| **Datadog Org** | https://app.datadoghq.com/organization-settings | |

---

**作成者**: システムコンサルタント
**最終更新日**: 2025年12月30日
**バージョン**: 1.0
